#apiVersion: external-secrets.io/v1beta1
#kind: ExternalSecret
#metadata:
#  name: jenkins-secrets
#  namespace: jenkins # match with app namespace
#spec:
#  refreshInterval: "1h"
#  secretStoreRef:
#    kind: ClusterSecretStore
#    name: jenkins-secrets # must match secret store metadata name
#  target:
#    name: my-app-secret
#    creationPolicy: Owner # managed by ESO
#  data:
#    - secretKey: role-arn # arbitrary key name
#      remoteRef:
#        key: "argocd_irsa_arn"  # The path to SSM parameter