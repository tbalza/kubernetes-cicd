apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins # crucial for configMapGenerator

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 5.1.6  # Chart v5.1.6, App v2.440.3
    releaseName: jenkins
    namespace: jenkins
    includeCRDs: true # check if necessary ( as it should be defined in values.yaml)
#    valuesMerge: merge # how to treat valuesInline with respect to Values. Legal values: ‘merge’, ‘override’, ‘replace’. Defaults to ‘override’.
    valuesFile: values.yaml
#    additionalValuesFiles:
#    - casc-config-h.yaml

resources:
# - secrets2.yaml # load ECR repo dynamically # not really a secret, maybe configmap is more adequate # check
  - ingress.yaml # upstream helm ingress is stringent/finicky. kustomize is going to patch environments anyway

#configMapGenerator:
#- name: jenkins-scripts
#  files:
#  - Jenkinsfile.groovy # SCM Sync Configuration Plugin, which can help version control Jenkins job configurations.