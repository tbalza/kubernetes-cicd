apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: jenkins
    repo: https://charts.jenkins.io
    version: 5.1.6  # Chart v5.1.6, App v2.440.3
    releaseName: jenkins
    namespace: jenkins
    includeCRDs: true # check if necessary ( as it should be defined in values.yaml)
    valuesFile: values.yaml

resources:
  - casc-configmap.yaml  # ConfigMap containing the JCasC configuration

# Manage secrets through Kustomize
#secretGenerator:
#  - name: jenkins-secrets
#    files:
#      - secrets.txt  # Ensure secrets are managed securely
#    options:
#      disableNameSuffixHash: true

# Customize labels for tracking resources in your deployment
commonLabels:
  app: jenkins