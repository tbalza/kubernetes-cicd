apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: jenkins-secrets
  namespace: jenkins
spec:
  provider:
    aws:
      service: ParameterStore # SystemManager
      region: us-east-1  # AWS region specific, pending dynamic value
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets # must match serviceAccount name (defined in helm chart)
            namespace: kube-system # must match ESO namespace
