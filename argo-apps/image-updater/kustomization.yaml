apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

helmCharts:
  - name: argocd-image-updater
    repo: https://argoproj.github.io/argo-helm
    version: 0.11.0 # Chart 0.11.0, App 0.14.0 #Chart 0.10.2, App v0.13.1
    releaseName: argocd-image-updater
    namespace: argocd
    includeCRDs: false
    valuesFile: values.yaml
    valuesMerge: merge

resources:
  - rbac.yaml
  - serviceaccount.yaml
  - secrets.yaml

#patches:
#  - target:
#      group: ""
#      version: v1
#      kind: ServiceAccount
#      name: argocd-image-updater
#      namespace: argocd
#    patch: |-
#      - op: add
#        path: /metadata/annotations/eks.amazonaws.com~1role-arn
#        value: 'arn:aws:iam::${ARGOCD_AWS_ACCOUNT}:role/ImageUpdaterRole' # 350206045032